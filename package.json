{
  "name": "municipal-budget-intelligence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "npx serve DASHBOARD",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "windows:dev": "npx tauri dev --config windows/src-tauri/tauri.conf.json",
    "windows:build": "npx tauri build --config windows/src-tauri/tauri.conf.json"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@tauri-apps/cli": "^2.0.0"
  }
}
